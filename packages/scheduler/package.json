{
  "name": "@karyakram/scheduler",
  "version": "0.0.0",
  "private": true,
  "description": "Multi-replica scheduler: leader election via Postgres advisory lock, reaper failover.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "scheduler": "dotenv -e ../../.env -- tsx src/bin/run-scheduler.ts",
    "demo:m6": "dotenv -e ../../.env -- tsx src/bin/demo-m6.ts"
  },
  "dependencies": {
    "@karyakram/core": "workspace:*",
    "@karyakram/db": "workspace:*",
    "@karyakram/worker-sdk": "workspace:*",
    "pg": "^8.23.0",
    "pino": "^10.3.1"
  },
  "devDependencies": {
    "@types/pg": "^8.21.0",
    "dotenv-cli": "^11.0.0"
  }
}
